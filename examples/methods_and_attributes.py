# Objects consist of methods and attributes.
# Methods are merely functions that belong to objects. (and thus renamed to methods)
# Attributes are variables that belong to objects.
# A variable references an object, and attribute is something within an object that references another object.
